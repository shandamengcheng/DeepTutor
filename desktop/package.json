{
  "name": "deeptutor-desktop",
  "version": "0.2.0",
  "description": "Local-first macOS desktop shell for DeepTutor",
  "private": true,
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test test/*.test.js",
    "package:mac": "node scripts/package-mac.js"
  },
  "devDependencies": {
    "electron": "44.2.0"
  }
}
